```mermaid
sequenceDiagram
    autonumber
    participant Spring as Spring主线程 (启动容器)
    participant Async as 异步回灌线程 (ForkJoinPool)
    participant DB as MySQL (KnowPostMapper)
    participant ES as Elasticsearch
    participant External as OSS/Redis (外部依赖)

    Spring->>Spring: 触发 @PostConstruct 
    Spring->>Async: 提交异步任务 CompletableFuture.runAsync()
    Note left of Spring: 主线程瞬间返回，<br/>Spring Boot 顺利启动，不被阻塞！
    
    activate Async
    Async->>ES: count(INDEX) 检查是否已有数据
    ES-->>Async: 返回 0 (需回灌)
    
    loop while(true) 游标分页拉取
        Async->>DB: listFeedPublic(limit:500, offset)
        DB-->>Async: 返回 500 条简要记录
        
        Async->>Async: 创建全新的 BulkRequest.Builder
        
        loop for (KnowPostFeedRow r : rows)
            Note over Async, External: 🛡️ 局部 try-catch 护城河开始
            
            Async->>DB: findDetailById(r.getId())
            DB-->>Async: 返回单条详情
            
            Async->>External: fetchContentSafe (带3秒超时)
            External-->>Async: 返回正文或超时
            
            Async->>External: counterService.getCounts (查点赞/收藏)
            External-->>Async: 返回计数或异常
            
            alt 组装成功
                Async->>Async: 加入 bulkBuilder.operations(...)
            else 抛出异常 (如Redis超时)
                Async->>Async: catch (Exception e) 记录日志
                Note over Async: 💥 核心亮点：异常被内部消化，<br/>continue 下一条，绝不中断整个 for 循环！
            end
        end
        
        Note over Async, ES: 🚀 性能亮点：跳出 for 循环后，只 build() 一次
        Async->>ES: es.bulk(bulkRequest) 发送 500 条数据的聚合请求
        ES-->>Async: 返回 BulkResponse
        
        Async->>Async: offset += rows.size() 更新游标
    end
    
    Async->>Async: 打印回灌成功日志
    deactivate Async