````mermaid
flowchart TD
    Start([assembleFromCache 被调用]) --> Step1["第一步: 从 Redis List 获取 ID 列表<br>LRANGE idsKey 0 size-1"]
    Step1 --> Step2["第二步: 获取 hasMore 标记<br>GET hasMoreKey"]
    Step2 --> CheckEmpty{ID 列表为空?}
    CheckEmpty --|是| --> ReturnNull1([返回 null<br>触发回源])
    
    CheckEmpty --|否| --> Step3["第三步: 构造片段缓存 Key<br>feed:item:{id} x N"]
    Step3 --> Step4["第四步: 批量获取元数据<br>MGET 一次获取所有 JSON"]
    Step4 --> Step5["第五步: 解析 JSON<br>全量校验完整性"]
    
    Step5 --> CheckMissing{有片段缺失?}
    CheckMissing --|是| --> ReturnNull2([返回 null<br>触发回源])
    
    CheckMissing --|否| --> Step6["第六步: 填充计数和用户状态"]
    
    Step6 --> Sub6a["6.1 查询公共计数<br>likeCount, favoriteCount"]
    Sub6a --> Sub6b["6.2 查询用户状态<br>liked, faved"]
    Sub6b --> Sub6c["6.3 组装完整响应<br>基础信息 + 计数 + 状态"]
    
    Sub6c --> Step7["第七步: 判断 hasMore<br>优先缓存标记 → 满页兜底"]
    Step7 --> Step8["第八步: 返回 FeedPageResponse"]
