```mermaid
sequenceDiagram
    participant C as 客户端
    participant Controller as AuthController
    participant Service as AuthService
    participant Log as LoginLogService
    
    C->>Controller: POST /api/auth/login<br/>{username, password}
    Controller->>Controller: resolveClient(request)
    Controller->>Controller: extractClientIp(request)
    Controller-->>Controller: ClientInfo(ip, ua)
    
    Controller->>Service: login(request, clientInfo)
    Service->>Service: 验证用户凭证
    Service->>Service: 生成令牌对
    Service->>Service: 存储刷新令牌
    
    Service->>Log: record(userId, identifier, "PASSWORD",<br/>clientInfo.ip(), clientInfo.userAgent(), "SUCCESS")
    Log->>Log: 存储登录日志
    
    Service-->>Controller: AuthResponse
    Controller-->>C: 返回令牌对