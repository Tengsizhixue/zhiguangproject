```mermaid
stateDiagram-v2
    [*] --> 接收JWT令牌
    接收JWT令牌 --> 格式解析
    格式解析 --> 格式验证
    格式验证 --> Header解码: 格式正确
    格式验证 --> 解码失败: 格式错误
    
    Header解码 --> Payload解码
    Payload解码 --> 时间验证
    
    时间验证 --> 检查过期时间
    检查过期时间 --> 检查生效时间: 未过期
    检查过期时间 --> 令牌过期: 已过期
    
    检查生效时间 --> 签名验证: 已生效
    检查生效时间 --> 令牌未生效: 未生效
    
    签名验证 --> 读取公钥
    读取公钥 --> 计算签名
    计算签名 --> 比较签名
    
    比较签名 --> 构建Jwt对象: 签名匹配
    比较签名 --> 签名无效: 签名不匹配
    
    构建Jwt对象 --> 返回结果
    返回结果 --> [*]
    
    解码失败 --> [*]
    令牌过期 --> [*]
    令牌未生效 --> [*]
    签名无效 --> [*]
    
    note right of 格式验证
        验证JWT三部分结构
        验证Base64Url编码
    end note
    
    note right of 时间验证
        检查exp（过期时间）
        检查nbf（生效时间）
        检查iat（签发时间）
    end note
    
    note right of 签名验证
        使用RSA公钥验证
        防止令牌篡改
        确保令牌来源可信
    end note
