深度解析：这个类到底在干什么？
这个类主要通过两个 Bean (securityFilterChain 和 corsConfigurationSource) 完成了五件大事：

第一件：确定“安检规则” (SecurityFilterChain)
代码中的 http.csrf(...) 到 .build() 就是在构建这套规则：

关闭 CSRF & 开启 CORS：因为你用的是 JWT（令牌）而不是 Cookie，所以不需要防 CSRF（防跨站伪造）。
但是因为前端和后端通常不在同一个端口（跨域），必须开启 CORS，否则浏览器会拦截数据。
设置“失忆模式” (Stateless Session)：告诉 Spring Security：“不要记住用户！不要创建 HttpSession！”。
这意味着服务器不存状态，用户必须每次请求都带着 Token（令牌）来证明自己。这是 JWT 架构的基石。
划定“白名单” (authorizeHttpRequests)：谁能直接进？ 运维监控 (/actuator/**)、Swagger文档、首页信息流、登录注册接口。
这些不需要查票，直接放行 (permitAll)。剩下的怎么办？ 除了白名单里的，其他人必须持有有效的“门票”（Token）才能进 (authenticated)。
连接“验票机” (OAuth2 Resource Server)：这就是 JWT 生效的地方。
它会自动截获 HTTP Header 里的 Authorization: Bearer xxx，并解析它。如果 Token 是伪造的或过期的，直接拒之门外。
第二件：搞定“浏览器保安” (CorsConfigurationSource)
浏览器出于安全考虑，默认禁止不同域名/端口之间的数据传输。 这个 Bean 就是写给浏览器的一封“介绍信”：

“我是后端，我允许所有来源 (*) 的人访问我。”
“我允许带 Authorization 头（里面装Token）。”
“我允许 GET, POST, PUT, DELETE 等操作。”
如果没有这个类
后果：系统将进入“默认封锁”状态，且完全不适用你的业务。

所有接口全被封锁： Spring Security 的默认策略是“宁可错杀三千，不可放过一个”。
你写的所有 Controller 接口（包括登录、注册），访问时都会报错 401 Unauthorized，或者跳转到一个默认的丑陋登录页面。
用户根本没法注册，因为注册接口也被拦截了，死循环。
前端跨域报错 (CORS Error)： 你的前端（Vue/React）在调用后端时，浏览器控制台会一片红，提示 CORS 被拒绝。因为默认是不允许跨域的。
POST 请求全部失败 (CSRF)： 默认开启 CSRF 保护。
你用 Postman 发送 POST 请求会被拦截，除非你搞到了隐藏的 CSRF Token（但这在前后端分离架构里是不现实的）。
使用 Session 而非 JWT： 系统会试图创建 Cookie 和 Session，这与你想要的“无状态 JWT”架构背道而驰。
